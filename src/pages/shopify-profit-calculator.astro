---
import Base from '../layouts/Base.astro';

const faqSchema = JSON.stringify({
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    { "@type": "Question", "name": "What is a good profit margin for a Shopify store?",
      "acceptedAnswer": { "@type": "Answer", "text": "A healthy Shopify store net margin is 10–20%. Above 20% is strong. Below 10% signals cost structure problems. Average ecommerce margins run 7–15% net, though digital products and high-AOV stores often hit 30%+." } },
    { "@type": "Question", "name": "How do I calculate my true Shopify profit?",
      "acceptedAnswer": { "@type": "Answer", "text": "Net profit = Revenue − COGS − Payment processing fees − Shipping costs − Shopify plan fee − App subscriptions − Marketing spend − Return handling costs. This calculator does all that math using 2026 rates." } },
    { "@type": "Question", "name": "What percentage does Shopify take per sale in 2026?",
      "acceptedAnswer": { "@type": "Answer", "text": "Shopify Payments rates: Basic 2.9% + $0.30, Shopify plan 2.7% + $0.30, Advanced 2.5% + $0.30, Plus 1.8% + $0.30. Use a third-party processor and Shopify adds extra: 2% (Basic), 1% (Shopify plan), 0.5% (Advanced)." } },
    { "@type": "Question", "name": "How much do Shopify apps cost per month?",
      "acceptedAnswer": { "@type": "Answer", "text": "Average Shopify merchants spend $100–$300/month on apps. Stores at $10K+/mo often spend $200–$500/month across email, reviews, upsells, and analytics. That is typically 1–3% of revenue." } },
    { "@type": "Question", "name": "What is Shopify's 3rd-party transaction fee?",
      "acceptedAnswer": { "@type": "Answer", "text": "If you don't use Shopify Payments, Shopify charges: 2% extra on Basic, 1% on Shopify plan, 0.5% on Advanced, 0.15% on Plus. On a $50K/mo store on Basic with Stripe, that is $1,000/month in extra fees." } },
  ]
});

const appSchema = JSON.stringify({
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": "Shopify Profit Calculator",
  "description": "Free calculator for Shopify store net profit, gross margin, and cost breakdown. Includes 2026 rates for all plans and processors.",
  "applicationCategory": "BusinessApplication",
  "operatingSystem": "Web",
  "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" },
  "url": "https://shopify-fee-calc.com/shopify-profit-calculator/"
});

const faqs = [
  { q: "What is a good profit margin for a Shopify store?",
    a: "A healthy Shopify net margin is 10–20%. Above 20% is strong. Below 10% signals cost structure problems. Average ecommerce margins run 7–15% net, though digital products and high-AOV stores often hit 30%+." },
  { q: "How do I calculate my true Shopify profit?",
    a: "Net profit = Revenue − COGS − Payment fees − Shipping − Plan fee − Apps − Marketing − Returns. This calculator handles all of that using 2026 rates automatically." },
  { q: "What percentage does Shopify take per sale in 2026?",
    a: "Shopify Payments rates in 2026: Basic 2.9%+30¢, Shopify plan 2.7%+30¢, Advanced 2.5%+30¢, Plus 1.8%+30¢. Use a third-party processor and Shopify adds extra: 2% on Basic, 1% on Shopify plan, 0.5% on Advanced." },
  { q: "How much do Shopify apps cost per month on average?",
    a: "Average Shopify merchants spend $100–$300/month on apps. Stores doing $10K+/mo often spend $200–$500/month across email marketing, reviews, upsells, and analytics. Typically 1–3% of revenue." },
  { q: "What is Shopify's 3rd-party transaction fee?",
    a: "If you don't use Shopify Payments, Shopify charges: 2% extra on Basic, 1% on Shopify plan, 0.5% on Advanced, 0.15% on Plus. On a $50K/mo store with Stripe on Basic plan, that's $1,000/month in extra fees alone." },
];
---
<Base
  title="Shopify Profit Calculator — Real Net Profit After All Fees (Free 2026)"
  description="Calculate your Shopify store's true net profit: COGS, payment fees, plan cost, apps, shipping, returns, and marketing. Free, instant, 2026 rates."
  noWebAppSchema={true}
>
  <script type="application/ld+json" set:html={faqSchema} />
  <script type="application/ld+json" set:html={appSchema} />

  <!-- Tailwind safelist for dynamically applied classes -->
  <div class="hidden bg-blue-50 dark:bg-blue-950 border-2 border-blue-600 dark:border-blue-500 bg-blue-600 text-white text-gray-600 dark:text-gray-300 bg-gray-50 dark:bg-gray-800 bg-orange-50 dark:bg-orange-950 text-orange-700 dark:text-orange-400 bg-yellow-50 dark:bg-yellow-950 text-yellow-700 dark:text-yellow-400 bg-green-50 dark:bg-green-950 text-green-700 dark:text-green-400 bg-red-50 dark:bg-red-950 text-red-700 dark:text-red-400 text-green-600 dark:text-green-400 text-red-600 dark:text-red-400 text-blue-600 dark:text-blue-400 border dark:border-gray-600 font-semibold text-gray-800 dark:text-gray-200 text-gray-600 dark:text-gray-400 text-gray-700 dark:text-gray-300 text-red-500 text-orange-500"></div>

  <h1 class="text-3xl font-bold mb-2">Shopify Profit Calculator</h1>
  <p class="text-gray-600 dark:text-gray-400 mb-8 max-w-2xl">
    Your real monthly P&amp;L — revenue minus COGS, payment fees, plan cost, apps, marketing, shipping, and returns.
    See exactly where every dollar goes. Uses 2026 Shopify rates.
  </p>

  <div class="flex flex-col lg:flex-row gap-8 items-start">

    <!-- ── LEFT: Inputs ──────────────────────────────────────── -->
    <div class="flex-1 min-w-0 space-y-4">

      <!-- Block 1: Revenue -->
      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-xs uppercase tracking-widest text-blue-600 dark:text-blue-400 mb-4">1 — Revenue</h3>
        <div class="grid grid-cols-2 gap-4">
          <div>
            <label class="block text-sm font-medium mb-1 dark:text-gray-300" for="inp-rev">Monthly Revenue ($)</label>
            <input id="inp-rev" type="number" min="0" step="100" value="50000" lang="en" inputmode="decimal"
              class="w-full border dark:border-gray-600 rounded-lg px-3 py-2 text-sm bg-white dark:bg-gray-800 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500" />
          </div>
          <div>
            <label class="block text-sm font-medium mb-1 dark:text-gray-300" for="inp-aov">
              Avg Order Value ($)
              <span class="tip" data-tip="Average revenue per order. Used to estimate number of orders per month.">?</span>
            </label>
            <input id="inp-aov" type="number" min="1" step="1" value="85" lang="en" inputmode="decimal"
              class="w-full border dark:border-gray-600 rounded-lg px-3 py-2 text-sm bg-white dark:bg-gray-800 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500" />
          </div>
        </div>
        <p class="text-xs text-gray-500 dark:text-gray-400 mt-2">
          Orders/month: <span id="d-orders" class="font-semibold text-gray-700 dark:text-gray-300">588</span>
        </p>
      </div>

      <!-- Block 2: COGS -->
      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-xs uppercase tracking-widest text-blue-600 dark:text-blue-400 mb-4">2 — Cost of Goods (COGS)</h3>
        <label class="block text-sm font-medium mb-2 dark:text-gray-300">
          COGS % of Revenue
          <span class="tip" data-tip="Product cost as % of revenue. Benchmarks: apparel 30–45%, electronics 50–65%, beauty 25–40%, jewelry 35–55%, home goods 40–55%.">?</span>
        </label>
        <div class="flex items-center gap-3">
          <input id="inp-cogs-range" type="range" min="0" max="90" step="1" value="35" class="flex-1 accent-blue-600" />
          <div class="flex items-center border dark:border-gray-600 rounded-lg overflow-hidden shrink-0">
            <input id="inp-cogs" type="number" min="0" max="90" step="1" value="35" lang="en" inputmode="decimal"
              class="w-14 px-2 py-1.5 text-sm text-right bg-white dark:bg-gray-800 dark:text-gray-100 focus:outline-none" />
            <span class="px-2 py-1.5 text-sm bg-gray-50 dark:bg-gray-700 dark:text-gray-400 border-l dark:border-gray-600">%</span>
          </div>
        </div>
        <p class="text-xs text-gray-500 dark:text-gray-400 mt-1.5">
          COGS this month: <span id="d-cogs" class="font-semibold text-gray-700 dark:text-gray-300">$17,500</span>
        </p>
      </div>

      <!-- Block 3: Plan -->
      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-xs uppercase tracking-widest text-blue-600 dark:text-blue-400 mb-4">3 — Shopify Plan</h3>
        <div class="grid grid-cols-2 gap-2 mb-4">
          <button data-plan="basic" class="plan-btn border dark:border-gray-600 rounded-xl p-3 text-left transition hover:border-blue-400">
            <div class="font-semibold text-sm dark:text-gray-100">Basic</div>
            <div class="text-xs text-gray-500 dark:text-gray-400">$39/mo · 2.9%+30¢</div>
          </button>
          <button data-plan="shopify" class="plan-btn border dark:border-gray-600 rounded-xl p-3 text-left transition hover:border-blue-400">
            <div class="font-semibold text-sm dark:text-gray-100">Shopify</div>
            <div class="text-xs text-gray-500 dark:text-gray-400">$105/mo · 2.7%+30¢</div>
          </button>
          <button data-plan="advanced" class="plan-btn border dark:border-gray-600 rounded-xl p-3 text-left transition hover:border-blue-400">
            <div class="font-semibold text-sm dark:text-gray-100">Advanced</div>
            <div class="text-xs text-gray-500 dark:text-gray-400">$399/mo · 2.5%+30¢</div>
          </button>
          <button data-plan="plus" class="plan-btn border dark:border-gray-600 rounded-xl p-3 text-left transition hover:border-blue-400">
            <div class="font-semibold text-sm dark:text-gray-100">Plus</div>
            <div class="text-xs text-gray-500 dark:text-gray-400">$2,300/mo · 1.8%+30¢</div>
          </button>
        </div>
        <div class="flex items-center gap-3">
          <span class="text-sm text-gray-600 dark:text-gray-400">Billing:</span>
          <div id="billing-wrap" class="flex rounded-lg border dark:border-gray-600 overflow-hidden text-sm">
            <button data-billing="monthly" class="billing-btn px-4 py-1.5 transition">Monthly</button>
            <button data-billing="annual" class="billing-btn px-4 py-1.5 transition">Annual (−25%)</button>
          </div>
          <span id="billing-plus-note" class="hidden text-xs text-gray-500 dark:text-gray-400">Plus pricing is fixed — no annual discount</span>
        </div>
        <p class="text-xs text-gray-500 dark:text-gray-400 mt-2">
          Plan cost: <span id="d-plan" class="font-semibold text-gray-700 dark:text-gray-300">$39/mo</span>
        </p>
      </div>

      <!-- Block 4: Payment -->
      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-xs uppercase tracking-widest text-blue-600 dark:text-blue-400 mb-4">4 — Payment Processor</h3>
        <div class="space-y-2">
          <button data-proc="shopify" class="proc-btn w-full text-left border dark:border-gray-600 rounded-xl px-4 py-3 transition hover:border-blue-400">
            <div class="flex justify-between">
              <span class="font-medium text-sm dark:text-gray-100">Shopify Payments</span>
              <span class="text-xs text-green-600 dark:text-green-400 font-medium">No extra fee</span>
            </div>
            <div class="text-xs text-gray-500 dark:text-gray-400 mt-0.5">Rate from your plan · no 3rd-party penalty</div>
          </button>
          <button data-proc="stripe" class="proc-btn w-full text-left border dark:border-gray-600 rounded-xl px-4 py-3 transition hover:border-blue-400">
            <div class="flex justify-between">
              <span class="font-medium text-sm dark:text-gray-100">Stripe</span>
              <span id="label-stripe" class="text-xs text-orange-500 font-medium">+2% penalty</span>
            </div>
            <div class="text-xs text-gray-500 dark:text-gray-400 mt-0.5">2.9% + $0.30/order + Shopify 3rd-party penalty</div>
          </button>
          <button data-proc="paypal" class="proc-btn w-full text-left border dark:border-gray-600 rounded-xl px-4 py-3 transition hover:border-blue-400">
            <div class="flex justify-between">
              <span class="font-medium text-sm dark:text-gray-100">PayPal</span>
              <span id="label-paypal" class="text-xs text-orange-500 font-medium">+2% penalty</span>
            </div>
            <div class="text-xs text-gray-500 dark:text-gray-400 mt-0.5">3.49% + $0.49/order + Shopify 3rd-party penalty</div>
          </button>
          <button data-proc="square" class="proc-btn w-full text-left border dark:border-gray-600 rounded-xl px-4 py-3 transition hover:border-blue-400">
            <div class="flex justify-between">
              <span class="font-medium text-sm dark:text-gray-100">Square</span>
              <span id="label-square" class="text-xs text-orange-500 font-medium">+2% penalty</span>
            </div>
            <div class="text-xs text-gray-500 dark:text-gray-400 mt-0.5">2.9% + $0.30/order + Shopify 3rd-party penalty</div>
          </button>
        </div>
        <p class="text-xs text-gray-500 dark:text-gray-400 mt-3">
          Total payment fees: <span id="d-payment" class="font-semibold text-gray-700 dark:text-gray-300">—</span>
        </p>
      </div>

      <!-- Block 5: Apps -->
      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-xs uppercase tracking-widest text-blue-600 dark:text-blue-400 mb-4">5 — Apps &amp; Subscriptions</h3>
        <div class="grid grid-cols-2 gap-4">
          <div>
            <label class="block text-sm font-medium mb-1 dark:text-gray-300" for="inp-apps">
              Paid Apps/month ($)
              <span class="tip" data-tip="Total monthly cost of all paid Shopify apps. Average store: $75–200/mo. Year-2 stores often reach $200–$500/mo with email, reviews, upsells, loyalty, and analytics apps.">?</span>
            </label>
            <input id="inp-apps" type="number" min="0" step="5" value="75" lang="en" inputmode="decimal"
              class="w-full border dark:border-gray-600 rounded-lg px-3 py-2 text-sm bg-white dark:bg-gray-800 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500" />
          </div>
          <div>
            <label class="block text-sm font-medium mb-1 dark:text-gray-300" for="inp-subs">
              Other Subscriptions ($)
              <span class="tip" data-tip="Tools outside the Shopify App Store: Gorgias, Klaviyo upgrades, Postscript, Zipify, Northbeam, Triple Whale, etc.">?</span>
            </label>
            <input id="inp-subs" type="number" min="0" step="5" value="50" lang="en" inputmode="decimal"
              class="w-full border dark:border-gray-600 rounded-lg px-3 py-2 text-sm bg-white dark:bg-gray-800 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500" />
          </div>
        </div>
      </div>

      <!-- Block 6: Marketing -->
      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-xs uppercase tracking-widest text-blue-600 dark:text-blue-400 mb-4">6 — Marketing</h3>
        <div class="grid grid-cols-2 gap-4">
          <div>
            <label class="block text-sm font-medium mb-1 dark:text-gray-300" for="inp-ads">
              Ad Spend/month ($)
              <span class="tip" data-tip="Meta/Google/TikTok/Pinterest ad spend + agency retainer. Exclude influencers — track those separately below.">?</span>
            </label>
            <input id="inp-ads" type="number" min="0" step="100" value="5000" lang="en" inputmode="decimal"
              class="w-full border dark:border-gray-600 rounded-lg px-3 py-2 text-sm bg-white dark:bg-gray-800 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500" />
          </div>
          <div>
            <label class="block text-sm font-medium mb-1 dark:text-gray-300" for="inp-influencer">
              Influencer / Content ($)
              <span class="tip" data-tip="Monthly spend on influencers, content creators, affiliates, and content production.">?</span>
            </label>
            <input id="inp-influencer" type="number" min="0" step="50" value="500" lang="en" inputmode="decimal"
              class="w-full border dark:border-gray-600 rounded-lg px-3 py-2 text-sm bg-white dark:bg-gray-800 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500" />
          </div>
        </div>
      </div>

      <!-- Block 7: Operations -->
      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-xs uppercase tracking-widest text-blue-600 dark:text-blue-400 mb-4">7 — Operations</h3>
        <div class="grid grid-cols-2 gap-4 mb-4">
          <div>
            <label class="block text-sm font-medium mb-1 dark:text-gray-300" for="inp-ship">
              Shipping/Order ($)
              <span class="tip" data-tip="Average outbound shipping cost per order including label + packaging materials. Enter 0 if you price shipping into your product cost.">?</span>
            </label>
            <input id="inp-ship" type="number" min="0" step="0.5" value="8" lang="en" inputmode="decimal"
              class="w-full border dark:border-gray-600 rounded-lg px-3 py-2 text-sm bg-white dark:bg-gray-800 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500" />
          </div>
          <div>
            <label class="block text-sm font-medium mb-1 dark:text-gray-300" for="inp-ret-pct">
              Returns Rate (%)
              <span class="tip" data-tip="Benchmarks: apparel 20–30%, footwear 25–30%, beauty 5–10%, electronics 8–12%, home goods 10–15%. Average ecommerce: 8–10%.">?</span>
            </label>
            <input id="inp-ret-pct" type="number" min="0" max="50" step="0.5" value="5" lang="en" inputmode="decimal"
              class="w-full border dark:border-gray-600 rounded-lg px-3 py-2 text-sm bg-white dark:bg-gray-800 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500" />
          </div>
        </div>
        <div>
          <label class="block text-sm font-medium mb-1 dark:text-gray-300" for="inp-ret-cost">
            Return Handling Cost/Return ($)
            <span class="tip" data-tip="Full cost per return: return shipping label ($5–$10) + staff inspection + repackaging + restocking + damaged inventory write-off. Typical range: $8–$18.">?</span>
          </label>
          <input id="inp-ret-cost" type="number" min="0" step="1" value="12" lang="en" inputmode="decimal"
            class="w-full border dark:border-gray-600 rounded-lg px-3 py-2 text-sm bg-white dark:bg-gray-800 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500" />
        </div>
      </div>

      <!-- Share -->
      <div class="flex items-center gap-3">
        <button id="share-btn"
          class="flex items-center gap-2 text-sm text-blue-600 dark:text-blue-400 border border-blue-300 dark:border-blue-700 rounded-lg px-4 py-2 hover:bg-blue-50 dark:hover:bg-blue-950 transition">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"/>
          </svg>
          Copy shareable link
        </button>
        <span id="share-confirm" class="hidden text-xs text-green-600 dark:text-green-400">&#10003; Copied!</span>
      </div>
    </div>

    <!-- ── RIGHT: Sticky results ──────────────────────────────── -->
    <div class="w-full lg:w-[360px] shrink-0">
      <div class="lg:sticky lg:top-6 space-y-4">

        <!-- P&L card -->
        <div class="border-2 border-blue-600 dark:border-blue-500 rounded-2xl overflow-hidden">
          <div class="bg-blue-600 dark:bg-blue-700 px-5 py-3">
            <h3 class="text-white font-semibold text-sm">Monthly P&amp;L</h3>
          </div>
          <div class="bg-white dark:bg-gray-900 px-5 py-4 text-sm space-y-1.5" id="results-breakdown">
            <!-- filled by JS -->
          </div>
          <div class="bg-gray-50 dark:bg-gray-800 border-t dark:border-gray-700 px-5 py-4">
            <div class="flex justify-between items-baseline mb-1">
              <span class="font-semibold">Net Profit</span>
              <span id="res-net" class="text-2xl font-bold text-blue-700 dark:text-blue-400">—</span>
            </div>
            <div class="flex justify-between text-sm text-gray-600 dark:text-gray-400 mb-1">
              <span>Margin</span>
              <span id="res-margin" class="font-semibold text-gray-800 dark:text-gray-200">—</span>
            </div>
            <div class="flex justify-between text-sm text-gray-600 dark:text-gray-400 mb-4">
              <span>Per order</span>
              <span id="res-per-order" class="font-medium text-gray-800 dark:text-gray-200">—</span>
            </div>
            <div id="res-benchmark" class="text-sm rounded-lg px-3 py-2 text-center font-medium"></div>
            <!-- Calculation details collapsible -->
            <details class="mt-3 group">
              <summary class="text-xs text-blue-600 dark:text-blue-400 cursor-pointer hover:underline list-none flex items-center gap-1">
                <svg class="w-3 h-3 transition-transform group-open:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
                </svg>
                See calculation details
              </summary>
              <div id="calc-details" class="mt-2 text-xs text-gray-500 dark:text-gray-400 space-y-0.5 font-mono bg-gray-50 dark:bg-gray-800 rounded-lg p-3 leading-relaxed"></div>
            </details>
          </div>
        </div>

        <!-- Pie chart card -->
        <div class="border dark:border-gray-700 rounded-2xl p-5 bg-white dark:bg-gray-900">
          <h3 class="text-sm font-semibold mb-4 text-gray-700 dark:text-gray-300">Where Your Revenue Goes</h3>
          <div class="flex items-center gap-5">
            <canvas id="pie-chart" width="120" height="120" class="shrink-0"></canvas>
            <div id="pie-legend" class="text-xs space-y-1.5 flex-1 min-w-0"></div>
          </div>
        </div>

      </div>
    </div>
  </div>

  <!-- ── Content Section 1: Profit Margin Benchmarks ──────────────────────── -->
  <div class="mt-16 max-w-3xl">
    <h2 class="text-2xl font-bold mb-4">What Is a Healthy Profit Margin for a Shopify Store?</h2>
    <p class="text-gray-700 dark:text-gray-300 mb-5 leading-relaxed">
      "Healthy margin" depends entirely on your product category, volume, and cost structure. A 15% net margin in apparel is excellent; in consumer electronics, it's a sign something is wrong. Use the table below to benchmark your numbers against category averages — then work backwards through your cost stack to find where you can improve.
    </p>

    <h3 class="text-lg font-semibold mb-3 text-gray-900 dark:text-gray-100">Profit Margin Benchmarks by Product Category (2026)</h3>
    <div class="overflow-x-auto rounded-xl border dark:border-gray-700 mb-6">
      <table class="w-full text-sm">
        <thead class="bg-gray-50 dark:bg-gray-800 text-left">
          <tr>
            <th class="px-4 py-3 font-semibold text-gray-700 dark:text-gray-300">Category</th>
            <th class="px-4 py-3 font-semibold text-gray-700 dark:text-gray-300">Gross Margin</th>
            <th class="px-4 py-3 font-semibold text-gray-700 dark:text-gray-300">Typical Net Margin</th>
            <th class="px-4 py-3 font-semibold text-gray-700 dark:text-gray-300">Primary cost driver</th>
          </tr>
        </thead>
        <tbody class="divide-y dark:divide-gray-700 text-gray-700 dark:text-gray-300">
          <tr class="hover:bg-gray-50 dark:hover:bg-gray-800/50">
            <td class="px-4 py-2.5 font-medium">Apparel &amp; Fashion</td>
            <td class="px-4 py-2.5">50–65%</td>
            <td class="px-4 py-2.5 text-green-700 dark:text-green-400 font-medium">12–22%</td>
            <td class="px-4 py-2.5">Returns (20–30% rate)</td>
          </tr>
          <tr class="hover:bg-gray-50 dark:hover:bg-gray-800/50">
            <td class="px-4 py-2.5 font-medium">Beauty &amp; Skincare</td>
            <td class="px-4 py-2.5">55–75%</td>
            <td class="px-4 py-2.5 text-green-700 dark:text-green-400 font-medium">18–30%</td>
            <td class="px-4 py-2.5">Customer acquisition (CAC)</td>
          </tr>
          <tr class="hover:bg-gray-50 dark:hover:bg-gray-800/50">
            <td class="px-4 py-2.5 font-medium">Consumer Electronics</td>
            <td class="px-4 py-2.5">20–40%</td>
            <td class="px-4 py-2.5 text-orange-700 dark:text-orange-400 font-medium">3–10%</td>
            <td class="px-4 py-2.5">COGS + warranty claims</td>
          </tr>
          <tr class="hover:bg-gray-50 dark:hover:bg-gray-800/50">
            <td class="px-4 py-2.5 font-medium">Jewelry &amp; Accessories</td>
            <td class="px-4 py-2.5">40–60%</td>
            <td class="px-4 py-2.5 text-green-700 dark:text-green-400 font-medium">15–28%</td>
            <td class="px-4 py-2.5">Marketing spend</td>
          </tr>
          <tr class="hover:bg-gray-50 dark:hover:bg-gray-800/50">
            <td class="px-4 py-2.5 font-medium">Home &amp; Garden</td>
            <td class="px-4 py-2.5">35–55%</td>
            <td class="px-4 py-2.5 text-yellow-700 dark:text-yellow-400 font-medium">8–18%</td>
            <td class="px-4 py-2.5">Shipping (bulky/heavy)</td>
          </tr>
          <tr class="hover:bg-gray-50 dark:hover:bg-gray-800/50">
            <td class="px-4 py-2.5 font-medium">Sports &amp; Outdoors</td>
            <td class="px-4 py-2.5">40–55%</td>
            <td class="px-4 py-2.5 text-yellow-700 dark:text-yellow-400 font-medium">10–20%</td>
            <td class="px-4 py-2.5">Seasonality + ad costs</td>
          </tr>
          <tr class="hover:bg-gray-50 dark:hover:bg-gray-800/50">
            <td class="px-4 py-2.5 font-medium">Food &amp; Supplements</td>
            <td class="px-4 py-2.5">50–70%</td>
            <td class="px-4 py-2.5 text-green-700 dark:text-green-400 font-medium">15–25%</td>
            <td class="px-4 py-2.5">Subscription churn</td>
          </tr>
          <tr class="hover:bg-gray-50 dark:hover:bg-gray-800/50">
            <td class="px-4 py-2.5 font-medium">Pet Products</td>
            <td class="px-4 py-2.5">45–65%</td>
            <td class="px-4 py-2.5 text-green-700 dark:text-green-400 font-medium">14–22%</td>
            <td class="px-4 py-2.5">App stack (loyalty)</td>
          </tr>
          <tr class="hover:bg-gray-50 dark:hover:bg-gray-800/50">
            <td class="px-4 py-2.5 font-medium">Digital Products</td>
            <td class="px-4 py-2.5">80–95%</td>
            <td class="px-4 py-2.5 text-green-700 dark:text-green-400 font-medium">30–60%</td>
            <td class="px-4 py-2.5">Marketing + support</td>
          </tr>
          <tr class="hover:bg-gray-50 dark:hover:bg-gray-800/50">
            <td class="px-4 py-2.5 font-medium">Dropshipping</td>
            <td class="px-4 py-2.5">15–35%</td>
            <td class="px-4 py-2.5 text-red-700 dark:text-red-400 font-medium">3–12%</td>
            <td class="px-4 py-2.5">Ad cost + supplier variance</td>
          </tr>
        </tbody>
      </table>
    </div>

    <h3 class="text-lg font-semibold mb-3 text-gray-900 dark:text-gray-100">5 Rules of Thumb for Shopify Margin Health</h3>
    <ol class="space-y-3 text-gray-700 dark:text-gray-300">
      <li class="flex gap-3">
        <span class="shrink-0 w-6 h-6 rounded-full bg-blue-100 dark:bg-blue-950 text-blue-700 dark:text-blue-400 flex items-center justify-center text-xs font-bold">1</span>
        <div><strong class="text-gray-900 dark:text-gray-100">The 3× rule:</strong> If your COGS is X, your selling price should be at least 3X to survive on paid ads. At 2.5X you're breakeven-or-loss territory on cold traffic.</div>
      </li>
      <li class="flex gap-3">
        <span class="shrink-0 w-6 h-6 rounded-full bg-blue-100 dark:bg-blue-950 text-blue-700 dark:text-blue-400 flex items-center justify-center text-xs font-bold">2</span>
        <div><strong class="text-gray-900 dark:text-gray-100">Payment fees are usually your #2 or #3 cost.</strong> At $50K/mo on Basic with Stripe, you pay ~$2,500/mo in processing — more than most merchants spend on apps and Shopify plan combined.</div>
      </li>
      <li class="flex gap-3">
        <span class="shrink-0 w-6 h-6 rounded-full bg-blue-100 dark:bg-blue-950 text-blue-700 dark:text-blue-400 flex items-center justify-center text-xs font-bold">3</span>
        <div><strong class="text-gray-900 dark:text-gray-100">App creep is real.</strong> Each new app you install typically adds $30–$100/month. Merchants who audit annually find 3–5 apps they pay for but don't actively use.</div>
      </li>
      <li class="flex gap-3">
        <span class="shrink-0 w-6 h-6 rounded-full bg-blue-100 dark:bg-blue-950 text-blue-700 dark:text-blue-400 flex items-center justify-center text-xs font-bold">4</span>
        <div><strong class="text-gray-900 dark:text-gray-100">At $20K+/mo, plan upgrades often pay for themselves.</strong> Moving from Basic ($39) to Advanced ($399) saves 0.4% per transaction — that's $80/mo savings per $20K in revenue, paying for the upgrade at $80K/mo.</div>
      </li>
      <li class="flex gap-3">
        <span class="shrink-0 w-6 h-6 rounded-full bg-blue-100 dark:bg-blue-950 text-blue-700 dark:text-blue-400 flex items-center justify-center text-xs font-bold">5</span>
        <div><strong class="text-gray-900 dark:text-gray-100">Returns are the silent margin killer in fashion.</strong> A 25% return rate with $15 handling cost destroys $3.75 of margin per order — which is why apparel brands price 10–15% higher than they'd need to otherwise.</div>
      </li>
    </ol>
  </div>

  <!-- ── Content Section 2: How the Calculator Works ──────────────────────── -->
  <div class="mt-14 max-w-3xl">
    <h2 class="text-2xl font-bold mb-4">How This Shopify Profit Calculator Works</h2>
    <p class="text-gray-700 dark:text-gray-300 mb-6 leading-relaxed">
      Every number in the results panel is derived from a transparent, step-by-step formula. Here's exactly how we get from revenue to net profit — no black boxes.
    </p>

    <div class="space-y-5">
      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-sm text-gray-900 dark:text-gray-100 mb-1">Step 1 — Estimate Monthly Orders</h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 mb-3">Monthly revenue divided by your average order value gives the order volume that drives all per-order cost calculations.</p>
        <pre class="text-xs bg-gray-50 dark:bg-gray-800 rounded-lg p-3 overflow-x-auto text-gray-700 dark:text-gray-300 font-mono"><code>Orders = Monthly Revenue ÷ Avg Order Value</code></pre>
      </div>

      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-sm text-gray-900 dark:text-gray-100 mb-1">Step 2 — Calculate COGS</h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 mb-3">Cost of goods is a direct percentage of revenue — your product cost plus any packaging and manufacturing overhead.</p>
        <pre class="text-xs bg-gray-50 dark:bg-gray-800 rounded-lg p-3 overflow-x-auto text-gray-700 dark:text-gray-300 font-mono"><code>COGS = Monthly Revenue × COGS%</code></pre>
      </div>

      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-sm text-gray-900 dark:text-gray-100 mb-1">Step 3 — Calculate Payment Processing Fees</h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 mb-3">This is the full cost: the processor's percentage rate, the per-transaction fee, and the Shopify third-party penalty (only applies if you don't use Shopify Payments).</p>
        <pre class="text-xs bg-gray-50 dark:bg-gray-800 rounded-lg p-3 overflow-x-auto text-gray-700 dark:text-gray-300 font-mono leading-relaxed"><code>Processing = (Revenue × Rate%) + (Orders × $0.30)
            + (Revenue × Shopify 3rd-party penalty%)</code></pre>
      </div>

      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-sm text-gray-900 dark:text-gray-100 mb-1">Step 4 — Calculate Gross Profit</h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 mb-3">Gross profit is what remains after direct variable costs. It funds all your fixed costs and gives the "upper bound" on how profitable your business can be.</p>
        <pre class="text-xs bg-gray-50 dark:bg-gray-800 rounded-lg p-3 overflow-x-auto text-gray-700 dark:text-gray-300 font-mono"><code>Gross Profit = Revenue − COGS − Processing Fees − Shipping</code></pre>
      </div>

      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-sm text-gray-900 dark:text-gray-100 mb-1">Step 5 — Deduct Operating Expenses (OPEX)</h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 mb-3">OPEX includes your Shopify plan, apps, marketing, and return handling costs. These are mostly fixed or semi-fixed costs that don't scale directly with individual orders.</p>
        <pre class="text-xs bg-gray-50 dark:bg-gray-800 rounded-lg p-3 overflow-x-auto text-gray-700 dark:text-gray-300 font-mono leading-relaxed"><code>OPEX = Plan Fee + Apps + Subscriptions
      + Marketing + (Orders × Return Rate × Return Cost)</code></pre>
      </div>

      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-sm text-gray-900 dark:text-gray-100 mb-1">Step 6 — Net Profit &amp; Margin</h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 mb-3">Net profit is the final number — what actually flows into your bank account after every cost is paid. Net margin expresses this as a percentage of revenue for easy benchmarking.</p>
        <pre class="text-xs bg-gray-50 dark:bg-gray-800 rounded-lg p-3 overflow-x-auto text-gray-700 dark:text-gray-300 font-mono leading-relaxed"><code>Net Profit = Gross Profit − OPEX
Net Margin = Net Profit ÷ Revenue × 100</code></pre>
      </div>
    </div>
  </div>

  <!-- ── Content Section 3: 7 Hidden Costs ────────────────────────────────── -->
  <div class="mt-14 max-w-3xl">
    <h2 class="text-2xl font-bold mb-4">7 Hidden Costs Quietly Killing Your Shopify Profit</h2>
    <p class="text-gray-700 dark:text-gray-300 mb-6 leading-relaxed">
      These fees aren't secret — they're in Shopify's pricing documentation. But they're scattered, percentage-based, and accumulate silently. Most merchants don't notice them until they do their first real P&amp;L analysis. Here are the seven most commonly missed.
    </p>

    <div class="space-y-4">
      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-gray-900 dark:text-gray-100 mb-1 flex items-center gap-2">
          <span class="text-red-500 font-bold">1.</span> Third-Party Transaction Fee
        </h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">If you use Stripe, PayPal, or any processor other than Shopify Payments, Shopify charges an additional fee on every transaction: 2% on Basic, 1% on Shopify plan, 0.5% on Advanced, 0.15% on Plus. On a $50,000/month store using Stripe on the Basic plan, that's <strong class="text-red-600 dark:text-red-400">$1,000/month</strong> in fees that disappear before you even see the money.</p>
        <p class="text-xs text-green-700 dark:text-green-400 mt-2 font-medium">Fix: Switch to Shopify Payments to eliminate this fee entirely.</p>
      </div>

      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-gray-900 dark:text-gray-100 mb-1 flex items-center gap-2">
          <span class="text-red-500 font-bold">2.</span> App Stack Bloat
        </h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">The average Shopify store at $10K+/month pays $200–$500/month in apps. The problem isn't any single app — it's that merchants install apps to solve problems, fix those problems, and then never uninstall the app. A quarterly app audit typically finds 3–5 apps costing $150–$300/month that are no longer used or could be consolidated.</p>
        <p class="text-xs text-green-700 dark:text-green-400 mt-2 font-medium">Fix: Audit your Shopify admin → Apps page every 90 days. Uninstall anything with zero usage in 30 days.</p>
      </div>

      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-gray-900 dark:text-gray-100 mb-1 flex items-center gap-2">
          <span class="text-red-500 font-bold">3.</span> Currency Conversion Fee (1.5%)
        </h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">Shopify charges 1.5% on every transaction where the customer's currency differs from your store's payout currency — even if you're using Shopify Payments. This is separate from your processing rate. A store doing 20% international revenue on $50K/month pays <strong class="text-red-600 dark:text-red-400">$150/month</strong> in conversion fees that most merchants never notice.</p>
        <p class="text-xs text-green-700 dark:text-green-400 mt-2 font-medium">Fix: In Shopify Markets, set up a payout currency that matches your largest international market to reduce conversions.</p>
      </div>

      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-gray-900 dark:text-gray-100 mb-1 flex items-center gap-2">
          <span class="text-red-500 font-bold">4.</span> True Return Cost vs. "Free Returns"
        </h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">Merchants offering free returns think they're just paying for a return label ($5–$8). The real cost per return is $8–$18 when you include labor for inspection, repackaging, restocking, and damaged-inventory write-offs. At a 20% return rate on 500 orders/month, that's 100 returns × $13 average = <strong class="text-red-600 dark:text-red-400">$1,300/month</strong> in hidden operational costs.</p>
        <p class="text-xs text-green-700 dark:text-green-400 mt-2 font-medium">Fix: Use a returns management platform to reduce processing time. Consider restocking fees for higher-return SKUs.</p>
      </div>

      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-gray-900 dark:text-gray-100 mb-1 flex items-center gap-2">
          <span class="text-red-500 font-bold">5.</span> Dimensional Weight (DIM Weight) Upcharges
        </h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">Carriers (UPS, FedEx, USPS Priority Mail) charge based on whichever is higher: actual weight or dimensional weight (L × W × H ÷ 139 for domestic). A lightweight product in a large box can cost 2–3× what you'd expect. Merchants who don't track DIM weight often find their actual shipping costs 15–40% higher than their per-order estimate.</p>
        <p class="text-xs text-green-700 dark:text-green-400 mt-2 font-medium">Fix: Right-size your packaging. Add custom box sizes in your carrier settings. Review DIM surcharges in your carrier invoices quarterly.</p>
      </div>

      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-gray-900 dark:text-gray-100 mb-1 flex items-center gap-2">
          <span class="text-red-500 font-bold">6.</span> Shopify Email Overage Charges
        </h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">Shopify Email includes 10,000 free emails/month, then $0.001 per email (1,000 emails = $1). A store with a 20,000-subscriber list sending 4 campaigns/month sends 80,000 emails — costing <strong class="text-red-600 dark:text-red-400">$70/month</strong> in email fees that often don't show up until the invoice. Merchants using Shopify Email instead of Klaviyo for large lists often overpay versus switching to a flat-rate plan.</p>
        <p class="text-xs text-green-700 dark:text-green-400 mt-2 font-medium">Fix: At 15K+ subscribers, compare Shopify Email overage cost vs. Klaviyo or Omnisend flat-rate plans.</p>
      </div>

      <div class="border dark:border-gray-700 rounded-xl p-5 bg-white dark:bg-gray-900">
        <h3 class="font-semibold text-gray-900 dark:text-gray-100 mb-1 flex items-center gap-2">
          <span class="text-red-500 font-bold">7.</span> Chargeback Fees ($15 per dispute)
        </h3>
        <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">Every chargeback costs $15 in fees — regardless of whether you win or lose the dispute. The industry average chargeback rate is 0.1–0.5% of transactions. A store processing 1,000 orders/month at 0.3% chargeback rate has 3 chargebacks/month = <strong class="text-red-600 dark:text-red-400">$45/month</strong>, plus the lost revenue on disputes you lose. High-risk categories (electronics, supplements) can hit 1%+ rates.</p>
        <p class="text-xs text-green-700 dark:text-green-400 mt-2 font-medium">Fix: Enable Shopify Protect (fraud analysis). Use clear delivery confirmation and proactive customer communication to reduce disputes.</p>
      </div>
    </div>
  </div>

  <!-- Related calculators -->
  <div class="mt-14">
    <h2 class="text-xl font-bold mb-5">Related Calculators</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">
      {[
        { href: '/shopify-hidden-fees-audit/', title: 'Hidden Fees Audit Tool', desc: 'Full 5-step audit of every Shopify cost' },
        { href: '/break-even/', title: 'Break-Even Calculator', desc: 'Units needed to cover fixed costs' },
        { href: '/profit-margin/', title: 'Profit Margin Calculator', desc: 'Per-product margin after all fees' },
        { href: '/shopify-fees/', title: 'Shopify Fee Calculator', desc: 'Transaction fees by plan and processor' },
        { href: '/free-shipping-threshold/', title: 'Free Shipping Threshold', desc: 'Find your break-even threshold' },
        { href: '/shipping-calculator/', title: 'Shipping Rate Calculator', desc: 'Compare USPS, UPS, FedEx rates' },
      ].map(({ href, title, desc }) => (
        <a href={href} class="border dark:border-gray-700 rounded-xl p-4 hover:border-blue-400 dark:hover:border-blue-500 transition group">
          <div class="font-semibold text-sm group-hover:text-blue-600 dark:group-hover:text-blue-400 dark:text-gray-100">{title}</div>
          <div class="text-xs text-gray-500 dark:text-gray-400 mt-1">{desc}</div>
        </a>
      ))}
    </div>
  </div>

  <!-- FAQ -->
  <div class="mt-14 max-w-3xl">
    <h2 class="text-2xl font-bold mb-6">Frequently Asked Questions</h2>
    <div class="space-y-3">
      {faqs.map(({ q, a }) => (
        <details class="border dark:border-gray-700 rounded-xl overflow-hidden group">
          <summary class="flex justify-between items-center px-5 py-4 cursor-pointer font-medium text-sm hover:bg-gray-50 dark:hover:bg-gray-800 transition list-none">
            <span>{q}</span>
            <svg class="w-4 h-4 text-gray-400 shrink-0 ml-3 transition-transform group-open:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
            </svg>
          </summary>
          <div class="px-5 pb-4 text-sm text-gray-600 dark:text-gray-400">{a}</div>
        </details>
      ))}
    </div>
  </div>
</Base>

<style>
  .tip {
    display: inline-flex; align-items: center; justify-content: center;
    width: 14px; height: 14px; border-radius: 50%;
    font-size: 9px; font-weight: 700; font-style: normal;
    background: #d1d5db; color: #374151; cursor: help;
    vertical-align: middle; margin-left: 3px;
  }
  :global(.dark) .tip { background: #374151; color: #d1d5db; }
</style>

<script>
const PLANS: Record<string, { monthly: number; annual: number; rate: number; perTxn: number; penalty: number }> = {
  basic:    { monthly: 39,   annual: 29,   rate: 0.029, perTxn: 0.30, penalty: 0.02 },
  shopify:  { monthly: 105,  annual: 79,   rate: 0.027, perTxn: 0.30, penalty: 0.01 },
  advanced: { monthly: 399,  annual: 299,  rate: 0.025, perTxn: 0.30, penalty: 0.005 },
  plus:     { monthly: 2300, annual: 2300, rate: 0.018, perTxn: 0.30, penalty: 0.0015 },
};

const PROCS: Record<string, { rate: number | null; perTxn: number | null; penalty: boolean }> = {
  shopify: { rate: null,   perTxn: null,  penalty: false },
  stripe:  { rate: 0.029,  perTxn: 0.30,  penalty: true },
  paypal:  { rate: 0.0349, perTxn: 0.49,  penalty: true },
  square:  { rate: 0.029,  perTxn: 0.30,  penalty: true },
};

const PIE_SLICES = [
  { key: 'cogs',    label: 'COGS',         color: '#3b82f6' },
  { key: 'mkt',     label: 'Marketing',    color: '#ef4444' },
  { key: 'ship',    label: 'Shipping',     color: '#a855f7' },
  { key: 'payment', label: 'Payment fees', color: '#f97316' },
  { key: 'apps',    label: 'Apps & subs',  color: '#eab308' },
  { key: 'plan',    label: 'Plan fee',     color: '#ec4899' },
  { key: 'returns', label: 'Returns',      color: '#14b8a6' },
  { key: 'net',     label: 'Net Profit',   color: '#22c55e' },
];

let plan = 'basic', billing = 'monthly', proc = 'shopify';

const ge = (id: string) => document.getElementById(id) as HTMLElement;
const gv = (id: string) => parseFloat((document.getElementById(id) as HTMLInputElement).value) || 0;

const fmt = (n: number) => '$' + Math.abs(n).toLocaleString('en-US', { maximumFractionDigits: 0 });
const fmtS = (n: number) => (n < 0 ? '−' : '') + fmt(n);
const pct = (n: number) => n.toFixed(1) + '%';

// COGS range/number sync
const cogsRange = document.getElementById('inp-cogs-range') as HTMLInputElement;
const cogsNum   = document.getElementById('inp-cogs') as HTMLInputElement;
cogsRange.addEventListener('input', () => { cogsNum.value = cogsRange.value; recalc(); });
cogsNum.addEventListener('input',   () => { cogsRange.value = cogsNum.value; recalc(); });

// Plan buttons
function activatePlan(p: string) {
  plan = p;
  document.querySelectorAll<HTMLElement>('.plan-btn').forEach(btn => {
    const on = btn.dataset.plan === p;
    btn.className = on
      ? 'plan-btn border-2 border-blue-600 dark:border-blue-500 bg-blue-50 dark:bg-blue-950 rounded-xl p-3 text-left transition'
      : 'plan-btn border dark:border-gray-600 rounded-xl p-3 text-left transition hover:border-blue-400';
  });
  const isPlus = p === 'plus';
  const wrap = document.getElementById('billing-wrap')!;
  const note = document.getElementById('billing-plus-note')!;
  wrap.classList.toggle('hidden', isPlus);
  note.classList.toggle('hidden', !isPlus);
  if (isPlus) billing = 'monthly';
  const penalty = PLANS[p].penalty;
  ['stripe','paypal','square'].forEach(k => {
    const el = document.getElementById(`label-${k}`);
    if (el) el.textContent = `+${(penalty * 100)}% penalty`;
  });
}

document.querySelectorAll<HTMLElement>('.plan-btn').forEach(btn =>
  btn.addEventListener('click', () => { activatePlan(btn.dataset.plan!); recalc(); })
);

// Billing buttons
function activateBilling(b: string) {
  billing = b;
  document.querySelectorAll<HTMLElement>('.billing-btn').forEach(btn => {
    const on = btn.dataset.billing === b;
    btn.className = on
      ? 'billing-btn px-4 py-1.5 text-sm bg-blue-600 text-white transition'
      : 'billing-btn px-4 py-1.5 text-sm text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition';
  });
}
document.querySelectorAll<HTMLElement>('.billing-btn').forEach(btn =>
  btn.addEventListener('click', () => { activateBilling(btn.dataset.billing!); recalc(); })
);

// Processor buttons
function activateProc(p: string) {
  proc = p;
  document.querySelectorAll<HTMLElement>('.proc-btn').forEach(btn => {
    const on = btn.dataset.proc === p;
    btn.className = on
      ? 'proc-btn w-full text-left border-2 border-blue-600 dark:border-blue-500 bg-blue-50 dark:bg-blue-950 rounded-xl px-4 py-3 transition'
      : 'proc-btn w-full text-left border dark:border-gray-600 rounded-xl px-4 py-3 transition hover:border-blue-400';
  });
}
document.querySelectorAll<HTMLElement>('.proc-btn').forEach(btn =>
  btn.addEventListener('click', () => { activateProc(btn.dataset.proc!); recalc(); })
);

// Canvas setup (once, HiDPI-aware)
const canvas = document.getElementById('pie-chart') as HTMLCanvasElement;
const dpr = Math.min(window.devicePixelRatio || 1, 2);
canvas.width  = 120 * dpr;
canvas.height = 120 * dpr;
canvas.style.width  = '120px';
canvas.style.height = '120px';
const ctx = canvas.getContext('2d')!;
ctx.scale(dpr, dpr);

function drawPie(vals: Record<string, number>, rev: number) {
  const slices = PIE_SLICES.map(s => ({ ...s, value: Math.max(0, vals[s.key] || 0) })).filter(s => s.value > 0);
  const total = slices.reduce((sum, s) => sum + s.value, 0);
  if (total <= 0) return;

  const cx = 60, cy = 60, r = 54, hole = 24;
  ctx.clearRect(0, 0, 120, 120);

  let angle = -Math.PI / 2;
  slices.forEach(s => {
    const sweep = (s.value / total) * 2 * Math.PI;
    ctx.beginPath();
    ctx.moveTo(cx, cy);
    ctx.arc(cx, cy, r, angle, angle + sweep);
    ctx.closePath();
    ctx.fillStyle = s.color;
    ctx.fill();
    angle += sweep;
  });

  ctx.beginPath();
  ctx.arc(cx, cy, hole, 0, 2 * Math.PI);
  ctx.fillStyle = document.documentElement.classList.contains('dark') ? '#111827' : '#ffffff';
  ctx.fill();

  const legend = ge('pie-legend');
  legend.innerHTML = slices.map(s => {
    const p = Math.round(s.value / total * 100);
    return `<div class="flex items-center gap-1.5 truncate">
      <span style="background:${s.color}" class="w-2.5 h-2.5 rounded-sm shrink-0"></span>
      <span class="text-gray-600 dark:text-gray-400 truncate">${s.label}</span>
      <span class="ml-auto font-medium text-gray-800 dark:text-gray-200 shrink-0">${p}%</span>
    </div>`;
  }).join('');
}

function recalc() {
  const rev   = gv('inp-rev');
  const aov   = Math.max(gv('inp-aov'), 1);
  const cogsPct = parseFloat(cogsNum.value) / 100;
  const apps  = gv('inp-apps');
  const subs  = gv('inp-subs');
  const ads   = gv('inp-ads');
  const inf   = gv('inp-influencer');
  const ship  = gv('inp-ship');
  const retP  = gv('inp-ret-pct') / 100;
  const retC  = gv('inp-ret-cost');

  const orders   = Math.round(rev / aov);
  const pl       = PLANS[plan];
  const pr       = PROCS[proc];
  const planCost = billing === 'annual' ? pl.annual : pl.monthly;
  const cogsAmt  = rev * cogsPct;

  const payRate   = pr.rate  ?? pl.rate;
  const payPerTxn = pr.perTxn ?? pl.perTxn;
  const penaltyAmt = pr.penalty ? rev * pl.penalty : 0;
  const paymentAmt = rev * payRate + orders * payPerTxn + penaltyAmt;

  const shipAmt    = orders * ship;
  const returnsAmt = orders * retP * retC;
  const mktAmt     = ads + inf;
  const appsAmt    = apps + subs;

  const gross = rev - cogsAmt - paymentAmt - shipAmt;
  const opex  = planCost + appsAmt + mktAmt + returnsAmt;
  const net   = gross - opex;
  const marginPct  = rev > 0 ? (net / rev * 100) : 0;
  const perOrder   = orders > 0 ? net / orders : 0;

  ge('d-orders').textContent  = orders.toLocaleString('en-US');
  ge('d-cogs').textContent    = fmt(cogsAmt);
  ge('d-plan').textContent    = fmt(planCost) + '/mo';
  ge('d-payment').textContent = fmt(paymentAmt) + ' (' + pct(paymentAmt / (rev || 1) * 100) + ')';

  const rows: Array<{ label?: string; value?: string; colorClass?: string; bold?: boolean; divider?: boolean }> = [
    { label: 'Revenue', value: fmt(rev), bold: true },
    { label: `COGS (${(cogsPct * 100).toFixed(0)}%)`, value: '−' + fmt(cogsAmt), colorClass: 'text-red-500' },
    { label: 'Payment fees', value: '−' + fmt(paymentAmt), colorClass: 'text-red-500' },
    { label: 'Shipping costs', value: '−' + fmt(shipAmt), colorClass: 'text-red-500' },
    { divider: true },
    { label: 'Gross Profit', value: fmtS(gross) + ' (' + pct(gross / (rev || 1) * 100) + ')', bold: true,
      colorClass: gross >= 0 ? 'text-blue-600 dark:text-blue-400' : 'text-red-600 dark:text-red-400' },
    { divider: true },
    { label: 'Shopify plan', value: '−' + fmt(planCost), colorClass: 'text-orange-500' },
    { label: 'Apps & subs', value: '−' + fmt(appsAmt), colorClass: 'text-orange-500' },
    { label: 'Marketing', value: '−' + fmt(mktAmt), colorClass: 'text-orange-500' },
    { label: 'Returns', value: '−' + fmt(returnsAmt), colorClass: 'text-orange-500' },
  ];

  ge('results-breakdown').innerHTML = rows.map(r => {
    if (r.divider) return '<div class="border-t dark:border-gray-700 my-1"></div>';
    return `<div class="flex justify-between items-center">
      <span class="${r.bold ? 'font-semibold text-gray-800 dark:text-gray-200' : 'text-gray-600 dark:text-gray-400'}">${r.label}</span>
      <span class="${r.bold ? 'font-semibold ' : ''}${r.colorClass || 'text-gray-700 dark:text-gray-300'}">${r.value}</span>
    </div>`;
  }).join('');

  const netEl = ge('res-net');
  netEl.textContent = fmtS(net);
  netEl.className = net >= 0
    ? 'text-2xl font-bold text-green-600 dark:text-green-400'
    : 'text-2xl font-bold text-red-600 dark:text-red-400';

  ge('res-margin').textContent   = pct(marginPct);
  ge('res-per-order').textContent = fmtS(perOrder) + '/order';

  const bench = ge('res-benchmark');
  if (marginPct < 0) {
    bench.textContent = '🔴 Operating at a loss — review all costs';
    bench.className = 'text-sm rounded-lg px-3 py-2 text-center font-medium bg-red-50 dark:bg-red-950 text-red-700 dark:text-red-400';
  } else if (marginPct < 10) {
    bench.textContent = '🟠 Below 10% — below industry average';
    bench.className = 'text-sm rounded-lg px-3 py-2 text-center font-medium bg-orange-50 dark:bg-orange-950 text-orange-700 dark:text-orange-400';
  } else if (marginPct < 20) {
    bench.textContent = '🟡 10–20% — average, room to optimize';
    bench.className = 'text-sm rounded-lg px-3 py-2 text-center font-medium bg-yellow-50 dark:bg-yellow-950 text-yellow-700 dark:text-yellow-400';
  } else {
    bench.textContent = `🟢 ${pct(marginPct)} — strong margin, ready to scale`;
    bench.className = 'text-sm rounded-lg px-3 py-2 text-center font-medium bg-green-50 dark:bg-green-950 text-green-700 dark:text-green-400';
  }

  // Calculation details collapsible
  const details = document.getElementById('calc-details');
  if (details) {
    const penLine = penaltyAmt > 0 ? `\n  + ${fmt(penaltyAmt)} (${(pl.penalty*100).toFixed(1)}% 3rd-party penalty)` : '';
    details.innerHTML = [
      `Orders: ${orders.toLocaleString('en-US')} = ${fmt(rev)} ÷ $${aov.toFixed(0)}`,
      `COGS: ${fmt(cogsAmt)} = ${(cogsPct*100).toFixed(0)}% × ${fmt(rev)}`,
      `Processing: ${fmt(paymentAmt)} = ${(payRate*100).toFixed(1)}% × ${fmt(rev)} + $${payPerTxn} × ${orders}${penLine}`,
      `Shipping: ${fmt(shipAmt)} = $${ship} × ${orders} orders`,
      `Gross profit: ${fmtS(gross)}`,
      `Returns: ${fmt(returnsAmt)} = $${retC} × ${(retP*100).toFixed(0)}% × ${orders}`,
      `OPEX: ${fmt(opex)} = plan + apps + marketing + returns`,
    ].map(line => `<div>${line}</div>`).join('');
  }

  drawPie({ cogs: cogsAmt, mkt: mktAmt, ship: shipAmt, payment: paymentAmt,
            apps: appsAmt, plan: planCost, returns: returnsAmt, net: Math.max(0, net) }, rev);
}

// Share link
ge('share-btn').addEventListener('click', () => {
  const d = btoa(JSON.stringify({
    rev: (document.getElementById('inp-rev') as HTMLInputElement).value,
    aov: (document.getElementById('inp-aov') as HTMLInputElement).value,
    cogs: cogsNum.value,
    apps: (document.getElementById('inp-apps') as HTMLInputElement).value,
    subs: (document.getElementById('inp-subs') as HTMLInputElement).value,
    ads: (document.getElementById('inp-ads') as HTMLInputElement).value,
    inf: (document.getElementById('inp-influencer') as HTMLInputElement).value,
    ship: (document.getElementById('inp-ship') as HTMLInputElement).value,
    ret: (document.getElementById('inp-ret-pct') as HTMLInputElement).value,
    retc: (document.getElementById('inp-ret-cost') as HTMLInputElement).value,
    plan, billing, proc,
  }));
  navigator.clipboard.writeText(location.origin + location.pathname + '?d=' + d).then(() => {
    const c = ge('share-confirm');
    c.classList.remove('hidden');
    setTimeout(() => c.classList.add('hidden'), 2000);
  });
});

// Load from URL
const urlParam = new URLSearchParams(location.search).get('d');
if (urlParam) {
  try {
    const data = JSON.parse(atob(urlParam));
    const setVal = (id: string, v: string | undefined) => {
      if (v !== undefined) (document.getElementById(id) as HTMLInputElement).value = v;
    };
    setVal('inp-rev', data.rev); setVal('inp-aov', data.aov);
    setVal('inp-apps', data.apps); setVal('inp-subs', data.subs);
    setVal('inp-ads', data.ads); setVal('inp-influencer', data.inf);
    setVal('inp-ship', data.ship); setVal('inp-ret-pct', data.ret); setVal('inp-ret-cost', data.retc);
    if (data.cogs) { cogsNum.value = data.cogs; cogsRange.value = data.cogs; }
    if (data.plan)    activatePlan(data.plan);
    if (data.billing) activateBilling(data.billing);
    if (data.proc)    activateProc(data.proc);
  } catch {}
}

// Input listeners
let debounce: ReturnType<typeof setTimeout>;
document.querySelectorAll<HTMLInputElement>('#inp-rev,#inp-aov,#inp-apps,#inp-subs,#inp-ads,#inp-influencer,#inp-ship,#inp-ret-pct,#inp-ret-cost').forEach(el =>
  el.addEventListener('input', () => { clearTimeout(debounce); debounce = setTimeout(recalc, 200); })
);

// Redraw pie on dark mode toggle
new MutationObserver(recalc).observe(document.documentElement, { attributes: true, attributeFilter: ['class'] });

// Init
activatePlan('basic');
activateBilling('monthly');
activateProc('shopify');
recalc();
</script>
